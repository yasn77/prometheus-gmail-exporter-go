apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-gmail-exporter-go.fullname" . }}
  labels:
    {{- include "prometheus-gmail-exporter-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-gmail-exporter-go.selectorLabels" . | nindent 4 }}
